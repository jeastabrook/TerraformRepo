# Local .terraform directories
**/.terraform/
**/.terraform/*

# .tfstate files (local state should not be committed)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude .tfvars files, which may contain sensitive data
*.tfvars
*.tfvars.json

# Ignore override files used for local modifications
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore Terraform plan output files
*.tfplan

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore the .terraform.lock.hcl file if you are managing provider versions
# explicitly or in a solo environment. If working in a team or needing strict
# provider version consistency, consider committing this file.
.terraform.lock.hcl

# Operating system specific files
.DS_Store